# Steam Analytics Worker

Приложение для сбора аналитики по предметам Steam Marketplace. Запускается каждый час и собирает данные о заказах на покупку и продажу.

## Возможности

- 🔄 Автоматический сбор данных каждый час
- 📊 Веб-интерфейс для просмотра аналитики
- 💾 Хранение данных в SQLite (легковесная БД)
- 🎯 API для получения данных
- 🐳 Готово к деплою на Render.com через Docker

## Требования

- .NET 8.0 SDK
- Docker (для деплоя)

## Локальный запуск

```bash
dotnet restore
dotnet run
```

Приложение будет доступно по адресу `http://localhost:5000`

## Деплой на Render.com

1. Создайте новый Web Service на Render.com
2. Выберите "Docker" как тип сервиса
3. Подключите ваш Git репозиторий
4. Укажите следующие настройки:
   - **Build Command**: (не требуется, Docker сам соберет)
   - **Start Command**: (не требуется, используется из Dockerfile)
   - **CPU**: 0.1
   - **RAM**: 512 MB
   - **Port**: 10000

## Использование

### Добавление предмета для отслеживания

1. Откройте веб-интерфейс
2. Введите название предмета и его Item ID из Steam
3. Нажмите "Добавить предмет"

### API Endpoints

- `GET /api/analytics/summary` - Общая статистика
- `GET /api/analytics/items` - Список всех предметов
- `GET /api/analytics/items/{itemId}/analytics` - Детальная аналитика по предмету
- `GET /api/analytics/items/{itemId}/orders?buyOrders=true&limit=100` - Заказы по предмету
- `POST /api/analytics/items` - Добавить новый предмет

### Пример запроса для добавления предмета

```bash
curl -X POST http://localhost:5000/api/analytics/items \
  -H "Content-Type: application/json" \
  -d '{"name": "AK-47 | Redline", "itemId": "12345678"}'
```

## Структура данных

- **Items** - Предметы для отслеживания
- **Orders** - Заказы на покупку/продажу
- **BuyOrders** / **SellOrders** - История заявок на покупку и продажу

## Оптимизация для ограниченных ресурсов

- Используется SQLite вместо полноценной БД
- Старые заказы (старше 1 часа) автоматически удаляются
- Минимальное использование памяти
- Оптимизированные запросы к БД

## Примечания

- Приложение собирает данные каждый час автоматически
- Данные хранятся в файле `data/steam_analytics.db`
- Для получения Item ID предмета используйте инструменты разработчика браузера на странице предмета в Steam Marketplace

